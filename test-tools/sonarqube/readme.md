```
тесты сонар
в сонаре создать профиль новый, потому что дефолтный нельзя редактировать
в нем уменьшаем coverage = 50%
врубаем quality_gate=true и запускаем
можно еще вручную сменить проекту профиль
не нашел параметр как сменить автоматом
можно через курл
curl -u admin:admin -X POST "http://localhost:9000/api/qualitygates/select?gateId=1&projectId=12"
```
