---
- name: "Check if Java is installed"
  shell: command -v java
  register: check_java
  ignore_errors: True
  changed_when: False
  check_mode: False

- name: include role java
  include_role:
    name: java
  when: check_java.rc != 0
